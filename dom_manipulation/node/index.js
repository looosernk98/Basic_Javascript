/*

 1. createElement: In an HTML document, the document.createElement() method 
             creates the HTML element specified by tagName, or an 
             HTMLUnknownElement if tagName isn't recognized.

             syntax :
             createElement(tagName)
             createElement(tagName, options)


 2. createTextNode: Creates a new Text node.

     syntax: 
     createTextNode(data)


 3. appendChild: it adds a node to the end of the list of children of a 
                 specified parent node.
                
   syatax:
    parent.appendChild(child)

*/

